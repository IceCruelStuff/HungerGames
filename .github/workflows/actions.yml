name: ON_PUSH

on:
  push:
    branches:
      - master

jobs:
  Document_Generator:
    runs-on: ubuntu-latest
    steps:
    - name: PhpDocumentor
      uses: gpupo/actions-phpDocumentor@1.0
      with:
        dir: './'
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
